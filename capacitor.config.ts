import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.josecedeno.appfeed',
  appName: 'appFeed',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
